{
  "name": "wazgo-backend",
  "version": "1.0.0",
  "description": "Backend för Waz Go: kontaktform, adminpanel, PostgreSQL, CSRF, mail",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "jest --runInBand",
    "backup": "node -e \"import('./utils/backup.js').then(m => m.backupDatabase()).then(() => console.log('Backup completed')).catch(console.error)\"",
    "verify-backup": "node -e \"import('./utils/backup.js').then(m => { const files = m.getBackupFiles(); if (files.length) { const latest = files.sort().pop(); m.verifyBackup('./backups/' + latest).then(() => console.log('Verification completed')).catch(console.error); } else console.log('No backups found'); })\"",
    "cleanup-backups": "node -e \"import('./utils/backup.js').then(m => m.cleanupOldBackups()).then(() => console.log('Cleanup completed')).catch(console.error)\""
  },
  "engines": {
  "node": "22.x"
},
  "dependencies": {
    "@playwright/test": "^1.55.1",
    "@sentry/node": "^10.15.0",
    "bcrypt": "^5.1.0",
    "check-updates": "^0.0.1",
    "compression": "^1.8.1",
    "connect-flash": "^0.1.1",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "csurf": "^1.11.0",
    "dotenv": "^17.2.2",
    "ejs": "^3.1.10",
    "eslint": "^9.36.0",
    "express": "^4.21.2",
    "express-rate-limit": "^8.1.0",
    "express-session": "^1.18.2",
    "express-validator": "^7.2.1",
    "helmet": "^8.1.0",
    "husky": "^9.1.7",
    "jsonwebtoken": "^9.0.2",
    "lint-staged": "^16.2.3",
    "multer": "^1.4.5-lts.1",
    "node-cache": "^5.1.2",
    "node-cron": "^3.0.3",
    "nodemailer": "^6.10.1",
    "pg": "^8.10.0",
    "pg-hstore": "^2.3.4",
    "prettier": "^3.6.2",
    "prom-client": "^15.1.3",
    "qrcode": "^1.5.4",
    "redis": "^5.8.3",
    "sequelize": "^6.32.1",
    "speakeasy": "^2.0.0",
    "swagger-jsdoc": "^6.2.8",
    "swagger-ui-express": "^5.0.1",
    "winston": "^3.17.0"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.28.3",
    "@jest/globals": "^30.2.0",
    "jest": "^30.2.0",
    "nodemon": "^2.0.22",
    "sequelize-mock": "^0.10.2",
    "supertest": "^7.1.4"
  }
}
