<!DOCTYPE html>
<html lang="sv">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="/admin/static/admin.css">
  <style>
    body { font-family: Arial, sans-serif; background: #f7f7f7; }
    .container { max-width: 900px; margin: 50px auto; background: #fff; padding: 20px; border-radius: 8px; box-shadow: 0 3px 10px rgba(0,0,0,0.1); }
    h1 { text-align: center; margin-bottom: 20px; }
    form { display: flex; flex-direction: column; gap: 15px; }
    label { font-weight: bold; }
    input[type="text"], textarea { padding: 8px; border: 1px solid #ccc; border-radius: 4px; width: 100%; }
    textarea { height: 200px; resize: vertical; }
    button { padding: 10px; background: #333; color: #fff; border: none; border-radius: 4px; cursor: pointer; }
    button:hover { background: #555; }
    nav { display: flex; justify-content: flex-end; margin-bottom: 20px; }
    nav a { margin-left: 15px; text-decoration: none; color: #333; }
    .flash-success { background: #e6ffed; color: #2b7a0b; padding: 10px; border-radius: 4px; margin-bottom: 10px; text-align: center; }
    .flash-error { background: #ffe6e6; color: #900; padding: 10px; border-radius: 4px; margin-bottom: 10px; text-align: center; }
    .current-image { max-width: 200px; margin-top: 10px; border: 1px solid #ccc; border-radius: 4px; }
  </style>
</head>
<body>
  <div class="container">
    <nav>
      <span><%= session.user.email %> (<%= session.user.role %>)</span>
      <a href="/admin/logout">Logga ut</a>
      <a href="/admin">Dashboard</a>
      <a href="/admin/contacts">Kontakter</a>
    </nav>

    <% if (flash.success && flash.success.length > 0) { %>
      <div class="flash-success"><%= flash.success[0] %></div>
    <% } %>
    <% if (flash.error && flash.error.length > 0) { %>
      <div class="flash-error"><%= flash.error[0] %></div>
    <% } %>

    <h1>Redigera sida: <%= page.title || page.slug %></h1>

    <form method="POST" action="/admin/pages/<%= page.slug %>" enctype="multipart/form-data">
      <input type="hidden" name="_csrf" value="<%= csrfToken %>">

      <label for="title">Titel</label>
      <input type="text" id="title" name="title" value="<%= page.title %>" required>

      <label for="content">Innehåll</label>
      <textarea id="content" name="content" required><%= page.content %></textarea>

      <label for="image">Bild (valfritt)</label>
      <input type="file" id="image" name="image" accept="image/*">
      <% if (page.imagePath) { %>
        <div>Nuvarande bild:</div>
        <img src="/<%= page.imagePath %>" alt="Page Image" class="current-image">
      <% } %>

      <button type="submit">Spara sidan</button>
    </form>
  </div>
</body>
</html>
