<!DOCTYPE html>
<html lang="sv">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="/admin/static/admin.css">
</head>
<body class="change-password">
  <div class="container">
    <h2>Ändra Lösenord</h2>

    <% if (flash.success && flash.success.length > 0) { %>
  <div class="flash-success"><%= flash.success[0] %></div>
<% } %>

<% if (flash.error && flash.error.length > 0) { %>
  <div class="flash-error"><%= flash.error[0] %></div>
<% } %>

    <form method="post" action="/admin/change-password" id="changeForm">
      <!-- CSRF token -->
      <input type="hidden" name="_csrf" value="<%= csrfToken %>">

      <label for="currentPassword">Nuvarande Lösenord</label>
      <input type="password" id="currentPassword" name="currentPassword" required autocomplete="current-password">

      <label for="newPassword">Nytt Lösenord</label>
      <input type="password" id="newPassword" name="newPassword" required autocomplete="new-password">

      <label for="confirmPassword">Bekräfta Nytt Lösenord</label>
      <input type="password" id="confirmPassword" name="confirmPassword" required autocomplete="new-password">

      <button type="submit">Ändra Lösenord</button>
    </form>

    <a href="/admin/dashboard">Tillbaka till Dashboard</a>
  </div>

  <!-- Confirmation Modal -->
  <div id="confirmModal" class="modal">
    <div class="modal-content">
      <h3>Säkerhetsbekräftelse</h3>
      <p>Ändring av lösenord kommer att logga ut dig från admin-panelen för säkerhet. Vill du fortsätta?</p>
      <div class="modal-buttons">
        <button id="cancelBtn">Avbryt</button>
        <button id="confirmBtn">Bekräfta</button>
      </div>
    </div>
  </div>

  <script>
    const form = document.getElementById('changeForm');
    const modal = document.getElementById('confirmModal');
    const confirmBtn = document.getElementById('confirmBtn');
    const cancelBtn = document.getElementById('cancelBtn');

    form.addEventListener('submit', function(e) {
      e.preventDefault();
      modal.style.display = 'flex';
    });

    confirmBtn.onclick = () => {
      form.submit();
    };

    cancelBtn.onclick = () => {
      modal.style.display = 'none';
    };

    window.onclick = (e) => {
      if (e.target === modal) {
        modal.style.display = 'none';
      }
    };
  </script>
</body>
</html>
